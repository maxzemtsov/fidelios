# v0.0.53

> Released: 2026-05-19

FideliOS-native merge coordination — parallel engineer agents in a company can
no longer merge into the trunk at the same time.

## Per-company merge slot

[#92](https://github.com/fideliosai/fidelios/pull/92) — when two engineer
agents finished PRs at once, both could merge into the trunk concurrently. Two
PRs each CI-green against an *older* trunk can land together and break it.
GitHub's merge queue solves this but requires an Enterprise plan on private
repos; this is the FideliOS-native equivalent — free, and uniform across every
company.

- A new `merge_locks` table grants one **merge slot** per company, enforced by
  a partial unique index. The service mirrors the issue checkout-lock: a
  conditional `INSERT ... ON CONFLICT DO NOTHING` is the whole concurrency
  primitive.
- `POST /api/companies/:companyId/merge-lock` acquires (or renews) the slot —
  non-blocking, returning `{"acquired":true}` or `{"acquired":false,"heldBy":…}`
  so the agent polls rather than failing. `DELETE` releases it; `GET` reports
  status.
- A stale slot — its 30-minute TTL expired, or its holder run terminal — is
  reclaimed on contention and by a reaper on the existing scheduler tick, so a
  crashed agent never blocks a company's merges.
- Engineer agents acquire the slot before `gh pr merge` and release after; the
  workflow is documented in the engineer `HEARTBEAT.md` "Merge Slot" section,
  repo `AGENTS.md` §11, and the fidelios skill.

## Internal

- Migration `0050_add_merge_locks.sql`.
- New tests: 13 real-DB cases in `merge-locks-service.test.ts` — acquire / deny
  / renew / release, holder-guard, force-release, stale + expired reclaim,
  reaper, per-company isolation, status. Full server suite green serially
  (628 passed).
- PR [#92](https://github.com/fideliosai/fidelios/pull/92) merged off `main`
  with green CI (`policy`, `verify`, `e2e`).
