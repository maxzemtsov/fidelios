## v0.0.7

- Added `fidelios update` command — check and install latest stable version
- Added `fidelios update --beta` for pre-release versions
- Auto-check for updates on `fidelios run` (non-blocking, 5s timeout)
