# v0.0.31

> Released: 2026-04-17

## Fixes

- **Root crash under Docker/VM/cloud root-ssh** — embedded-postgres refused to
  start with `Postgres does not support running as root`. FideliOS now
  auto-enables its `createPostgresUser` path when running as UID 0 so the
  embedded database boots unprivileged. Opt out with
  `FIDELIOS_EMBEDDED_POSTGRES_CREATE_USER=false` if you prefer to run as a
  non-root user.
- **Update check shows `0.0.0` on fresh installs** — `fidelios run` was reading
  `process.env.npm_package_version` which is only populated under `npm run`.
  The CLI now resolves its version from its own `package.json`.
- **fidelios.nl landing page** pointed at `ghcr.io/maxzemtsov/fidelios` (image
  never existed). Now `ghcr.io/fideliosai/fidelios`. Windows FAQ updated to
  describe the Node+CLI install (the Docker story for Windows was deprecated
  in v0.0.28).

## Docs

- [docs/start/quickstart.md](https://docs.fidelios.nl/start/quickstart)
  rewritten for non-technical readers — explicit Terminal / PowerShell
  instructions per platform.
- [docs/start/first-time-mac.md](https://docs.fidelios.nl/start/first-time-mac)
  (new) — Terminal-for-beginners walkthrough for users who have never opened
  Terminal.
- [docs/start/troubleshooting.md](https://docs.fidelios.nl/start/troubleshooting)
  (new) — every common failure mode with a copy-paste fix (PATH issues,
  `xcode-select`, Docker-hang, `EACCES`, root crash, stale `postmaster.pid`,
  adapter-not-in-PATH, dev-mode toggle not firing, ghcr visibility).
- [docs/start/install/linux.md](https://docs.fidelios.nl/start/install/linux)
  and [docs/start/install/cloud-vms.md](https://docs.fidelios.nl/start/install/cloud-vms)
  rewritten to match the actual installer behaviour (Docker vs CLI paths).
