[build]
  base    = "docs"
  command = "npx --yes mintlify@latest export && unzip -q export.zip -d _site"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
